services:
  backend:
    image: backend:v1.0
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    ports:
      - "8081:8000"
    volumes:
      - type: bind
        source: ./backend/data
        target: /app/data

  frontend:
    image: frontend:v1.0
    build:
      context: ./frontend
    env_file:
      - ./frontend/.env
    ports:
      - "8080:8000"
